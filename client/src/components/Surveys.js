import React from 'react'
import { connect } from 'mongoose';

const Surveys = () =>{
    return (
        <div style={{textAlign: 'center'}}>
            <h1>Emaily!</h1>
            User can take the survey here!
        </div>
    );
};

export default Surveys;


